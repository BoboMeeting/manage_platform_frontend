# Docker 部署环境变量样例
# 复制为 .env 后按需修改：cp .env.example .env

# 后端 .NET API 地址（容器内访问）
# - 同机部署（后端跑在宿主机）：http://host.docker.internal:5080
# - 后端也是 docker 容器并加入同一网络：http://backend:5080
# - 远程后端：http://10.0.0.5:5080
BACKEND_URL=http://host.docker.internal:5080

# 宿主机暴露端口（容器内固定 3000）
WEB_PORT=3000
